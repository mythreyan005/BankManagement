CREATE TABLE ACCOUNT_TBL (
Account_Number VARCHAR2(10) PRIMARY KEY,
Customer_Name  VARCHAR2(15),
Balance        NUMBER(10,2)
);

INSERT ALL
INTO ACCOUNT_TBL VALUES ('123599028','Reddy',80000)
INTO ACCOUNT_TBL VALUES ('923745947','Mahesh',0)
INTO ACCOUNT_TBL VALUES ('234492349','Dhanu',100)
INTO ACCOUNT_TBL VALUES ('291910483','Sam',500)
SELECT * FROM dual;

CREATE TABLE TRANSFER_TBL (
Transaction_ID NUMBER(4) PRIMARY KEY,
Account_Number VARCHAR2(10),
Benificiary_Account_Number VARCHAR2(10),
Transaction_Date DATE,

FOREIGN KEY (Account_Number) 
REFERENCES ACCOUNT_TBL(Account_Number),

FOREIGN KEY (Benificiary_Account_Number) 
REFERENCES ACCOUNT_TBL(Account_Number)
);


 author.setAuthorName(rs.getString("AUTHOR_NAME"));
                author.setContactNo(rs.getLong("CONTACT_NO"));
            }

            rs.close();
            ps.close();
            con.close();

        } catch (Exception e) {
            e.printStackTrace();
        }

        return author;
    }